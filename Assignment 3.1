1.#checking numbers equal or not
a=input("enter 1st number")
b=input("enter 2nd number")
if a==b :
print("a is equal to b")
else :
print("a is not equal to b")

2.adding of two numbers
a=input("enter first number")
b=input("enter second number")
add=a+b
print("adding two numbers gives",add)

3.finding sqaure root of number
import math
a=input("enter a num")
b=math.sqrt(a)
print("square is", b)

4.finding area of triangle
import math
print("area of triangle with sides   ")
a=input("enter length of first side   ")
b=input("enter the length of second side  ")
c=input("enter the lenght of third side  ")
p=(a+b+c)/2  #half of the perimeter
x=p-a
y=p-b
z=p-c
t=p*x*y*z
area=math.sqrt(t)
print("area of triangle is" , area)

5.solving quadratic equation
from math import sqrt
print("Quadratic function : (a * x^2) + b*x + c")
a = float(input("a: "))
b = float(input("b: "))
c = float(input("c: "))
r = b**2 - 4*a*c
if r > 0:
    num_roots = 2
    x1 = (((-b) + sqrt(r))/(2*a))     
    x2 = (((-b) - sqrt(r))/(2*a))
    print("There are 2 roots: %f and %f" % (x1, x2))
elif r == 0:
    num_roots = 1
    x = (-b) / 2*a
    print("There is one root: ", x)
else:
    num_roots = 0
    print("No roots, discriminant < 0.")
    exit()
	
